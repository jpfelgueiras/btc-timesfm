name: CI quality gates

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
  merge_group:

permissions:
  contents: read
  issues: read

jobs:
  quality-gates:
    name: Unit tests + quality
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-test.txt

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-test.txt

      - name: Restore flaky-test log
        uses: actions/cache/restore@v6
        with:
          path: .state/flaky-tests.json
          key: flaky-tests-${{ github.ref_name }}-${{ github.sha }}
          restore-keys: |
            flaky-tests-${{ github.ref_name }}-
            flaky-tests-main-

      - name: Unit tests with retry and quarantine
        id: unit
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          open_issues="$(gh issue list --state open --limit 1000 --json number --jq '.[].number' | xargs)"
          issue_args=""
          for issue in $open_issues; do
            issue_args="$issue_args --open-issue $issue"
          done
          PYTHONPATH=src python -m btc_timesfm.ops.flaky_tests \
            --log .state/flaky-tests.json \
            --quarantines .github/flaky-test-quarantine.json \
            --summary "$GITHUB_STEP_SUMMARY" \
            $issue_args -- \
            unittest-parallel -s tests -p 'test_*.py' -t . \
            --coverage --coverage-rcfile pyproject.toml --coverage-branch

      - name: Save flaky-test log
        if: always()
        uses: actions/cache/save@v6
        with:
          path: .state/flaky-tests.json
          key: flaky-tests-${{ github.ref_name }}-${{ github.sha }}

      - name: Coverage floor
        id: coverage
        if: always()
        continue-on-error: true
        run: |
          coverage report --format=markdown --fail-under=0 > coverage.md
          coverage report --fail-under=75

      - name: Add coverage to summary
        if: always() && hashFiles('coverage.md') != ''
        run: |
          echo '## Test coverage' >> "$GITHUB_STEP_SUMMARY"
          cat coverage.md >> "$GITHUB_STEP_SUMMARY"
          echo '' >> "$GITHUB_STEP_SUMMARY"
          echo 'Coverage floor: **75%** across the configured critical modules.' >> "$GITHUB_STEP_SUMMARY"

      - name: Ruff lint
        id: lint
        if: always()
        continue-on-error: true
        run: ruff check .

      - name: Ruff format check
        id: format
        if: always()
        continue-on-error: true
        run: ruff format --check .

      - name: Type check critical modules
        id: typecheck
        if: always()
        continue-on-error: true
        run: |
          mypy --follow-imports=skip \
            src/btc_timesfm/research/champion_challenger.py \
            src/btc_timesfm/research/champion_challenger_intervals.py \
            src/btc_timesfm/forecasting/conformal_calibration.py \
            src/btc_timesfm/research/correlation_backtest.py \
            src/btc_timesfm/forecasting/correlation_weighting.py \
            src/btc_timesfm/research/cross_asset_ablation.py \
            src/btc_timesfm/data/cross_asset_signals.py \
            src/btc_timesfm/research/derivatives_ablation.py \
            src/btc_timesfm/data/derivatives_signals.py \
            src/btc_timesfm/forecasting/diversified_model.py \
            src/btc_timesfm/ops/drift_detection.py \
            src/btc_timesfm/ops/flaky_tests.py \
            src/btc_timesfm/forecasting/experiment_manifest.py \
            src/btc_timesfm/forecasting/forecast_confidence.py \
            src/btc_timesfm/history/history_audit.py \
            src/btc_timesfm/history/history_backup.py \
            src/btc_timesfm/history/disaster_recovery.py \
            src/btc_timesfm/history/history_migrations.py \
            src/btc_timesfm/history/history_store.py \
            src/btc_timesfm/data/market_data_sources.py \
            src/btc_timesfm/data/market_data_validation.py \
            src/btc_timesfm/research/microstructure_ablation.py \
            src/btc_timesfm/data/microstructure_signals.py \
            src/btc_timesfm/ops/observability.py \
            src/btc_timesfm/research/performance_dashboard.py \
            src/btc_timesfm/ops/pipeline_health.py \
            src/btc_timesfm/x/post_to_x.py \
            src/btc_timesfm/ops/promotion_policy.py \
            src/btc_timesfm/ops/rollback_safeguards.py \
            src/btc_timesfm/research/regime_backtest.py \
            src/btc_timesfm/data/regime_detection.py \
            src/btc_timesfm/ops/schedule_guard.py \
            src/btc_timesfm/forecasting/statistical_significance.py \
            src/btc_timesfm/web/static_site.py \
            src/btc_timesfm/x/x_post_registry.py

      - name: Enforce quality gates
        if: always()
        env:
          UNIT: ${{ steps.unit.outcome }}
          COVERAGE: ${{ steps.coverage.outcome }}
          LINT: ${{ steps.lint.outcome }}
          FORMAT: ${{ steps.format.outcome }}
          TYPECHECK: ${{ steps.typecheck.outcome }}
        run: |
          failed=0
          for gate in UNIT COVERAGE LINT FORMAT TYPECHECK; do
            outcome="${!gate}"
            echo "$gate: $outcome"
            if [ "$outcome" != "success" ]; then
              failed=1
            fi
          done
          exit "$failed"

  # Compatibility aliases retained for the required status-check names on main.
  unit-tests-required-compat:
    name: Unit tests
    needs: quality-gates
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Mirror quality-gate result
        env:
          RESULT: ${{ needs.quality-gates.result }}
        run: |
          echo "Unit tests + quality: $RESULT"
          test "$RESULT" = "success"

  unit-tests-legacy-compat:
    name: unit-tests
    needs: quality-gates
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Mirror quality-gate result
        env:
          RESULT: ${{ needs.quality-gates.result }}
        run: |
          echo "Unit tests + quality: $RESULT"
          test "$RESULT" = "success"
